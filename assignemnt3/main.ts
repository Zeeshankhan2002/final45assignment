// storing the femous quote and its author

let quote:string = "nothing is impossible because impossible itself I AM POSSIBLE"

let author:string = "Zeeshan khan"

// printing the message

console.log(`"${quote}" - ${author}`);